compare - compare_values
//имя метода из OrderedList, надо обозначить, что сравниваем.

hash1, hash2 - get_hash
// из класса BloomFilter. В имени функции нет глагола, надо исправить. Кроме того, Функции идентичны, отличаются лишь базовой
случайной переменной, её можно передавать вторым параметром.

get_users - get_ad_pskov_users (скрипт в разработке)
//из скрипта по выборке пользователей из нашей рабочей ActiveDirectory. Домен на всю страну, плюс на серверах бывают локальные пользователи.
Надо указать, каким именно пользователей мы get.

get_dead_pcs - get_pskov_dead_pcs (скрипт в разработке)
//из скрипта по выборке ПК из нашей рабочей ActiveDirectory. Получить список ПК, которые не обращались к AD более полугода. Из первоначального
имени можно подумать, что собираются все "мёртвые души" домена, хотя на деле проверяется только OU Pskov.

do_a_channel - get_channel_videos
// из программы, выкачивающей видео подписок с ютуба, есть функция, смысл которой понятен только при чтении её содержимого. Не надо так.

get_time - load_time_from_LAST
//оттуда же, в константе _LAST задан путь к файлу, где хранится timestamp предыдущего времени запуска.
Функция считывает время именно из этого файла, считаю, надо уточнить, откуда получается время.
        
set_time - save_time_to_LAST
//оттуда же, сохраняем timestamp в файл

mkdir - check_and_make_dirs(self):
//оттуда же, проверяет, есть ли для каждого канала одноименная папка. Если такой нет - создаёт.

get_video_time - get_published_time
// оттуда же, проверять надо время публикации видео, а не некое "видеовремя".

initialize_time - initialize_LAST
// оттуда же, можно проинициализировать файл с временем отсчёта заранее, до первого запуска программы.

set_channel - set_channel_path
// оттуда же, функция задаёт не канал, который сейчас будет выкачиваться, а путь к папке канала, куда будет выкачиваться. Значит надо уточнять.

get_command - get_user_decision
// оттуда же, функция принимает в качестве команды решение пользователя о том, в каком режиме начинать работу программы.