6.1 filter - filter_bit_array
//в фильтре Блума/Блюма в качестве фильтра используется массив битов. Нужно более явно подчеркнуть, что целое двоичное число в качестве фильтра - это не
просто целое двоичное число, а массив битов.

each - inner_array_of_insides
//в методах класса PowerSet есть функции, сравнивающие, объединяющие, проверяющие и копирующие множества. У встроенного массива insides
в каждой ячейке хранится вложенный массив, надо подчеркнуть, что на каждом шаге мы проверяем очередной внутренний массив переменной insides.

com_url - user_input_URL
//в программе, выкачивающей видео подписок с ютуба, пользователь может ввести путь к каналу, которого нет в подписках, чтобы однократно скачать его целиком на локальный диск.
лучше всего указать,что это не просто URL (и что значит сокращение com? command? compose? combo?), а URL, который вводит пользователь.

com_channel - user_input_channel_name
//в той же программе пользователь вводит название канала (он же имя каталога, в который будут сохраняться видео с канала), лучше всего обозначить, что это имя, вводимое
непосредственно пользователем.

com_date - user_input_start_date
//в той же программе пользователь вводит, видео с какой даты будут сохраняться на локальный диск. Все видео старше указанное даты будут пропущены.

6.2. com_url - user_input_URL
//в программе, выкачивающей видео подписок с ютуба, пользователь может ввести путь к каналу, которого нет в подписках, чтобы однократно скачать его целиком на локальный диск.

ad_pskov_users (скрипт в разработке)
//из скрипта по выборке пользователей из нашей рабочей ActiveDirectory.

ad_dead_pcs (скрипт в разработке)
//из скрипта по выборке ПК из нашей рабочей ActiveDirectory. В этот список попадают ПК, которые не обращались к AD более полугода.

temp_dns_ip (скрипт в разработке)
//проверяет настройки DNS доменных ПК, в случае, если ip из данной переменной не содержится в перечнее допустимых ip, имя ПК попадает в список ПК, сетевые настройки
которых требуют ручного переназначения

6.3. В программе, выкачивающей видео подписок с ютуба, есть переменые URL, channel, channel_name, date, last_date и т.д., поэтому необходимо снабжать переменные,
в которые сохраняются данные, вводимые пользователем, префиксом user_input_ или его более короткой версией (вплоть до usr_, например):
com_url - user_input_URL
//релевантный пример из п. 6.1.

com_channel - user_input_channel_name
//релевантный пример из п. 6.1.

com_date - user_input_start_date
//релевантный пример из п. 6.1.

6.4 filter - filter_bit_array
//релевантный пример из п. 6.1.

f - file_with_timestamp
//в программе, выкачивающей видео подписок с ютуба, используется сохранение времени последнего использования программы в файл. Лучше указать, для чего используется файл.

com_url - user_input_URL
//релевантный пример из п. 6.1.

com_channel - user_input_channel_name
//релевантный пример из п. 6.1.

com_date - user_input_start_date
//релевантный пример из п. 6.1.