last10Files - lastTenFiles
// из какого-то старого java-файла институтской ещё поры. Даже не пытался разобраться, что там делается, это действительно ужасно смотрится.

max - maxFileNumber
// zip-файл с именем типа ws600045.zip. Отбрасывая 'ws60' и '.zip', получаем условный номер файла архива. Номер последнего архива (он наибольший из имеющихся).

min - minFileNumber
// аналогично, но наименьший из имеющихся.

toDelete - unpackedFilesToDelete
// объект класса ArrayList, содержащий имена уже распакованных файлов, предназначенных к удалению.

zip - unzipUtility
// объект самописного класса UnzipUtility, в исходном варианте не несёт смысла

asc - ascending_value
//числовая переменная из функции add класса OrderedList, равна 1, если порядок восходящий, -1 - если нисходящий.

index - calculated_index
//из функции seek_slot класса HashTable. Есть переменная basic_index - индекс, вычисленный функцией hash_fun, а есть переменная index, в которой происходит
перебор индексов в поиске незанятого. Возможно, имя index уже используется в какой-нибудь полезной библиотеке. Следует уточнить, что это за индекс.

key_hash - calculated_key_hash
//из функций put и get класса NativeDirectory. Причины плюс-минус те же: уточнение смысла, приведение длины к вилке 8-20.

dct - symbol_costs
// в задаче на оценку затрат тонера на печать того или иного текста из большой строки создаётся словарь стоимости печати каждого символа
в единицах тонера. Имя dct взято из соображений, что слово dict уже занято синтаксисом языка. Надо переделать.

down_count - descend_sequences_list
// в задаче про футболистов переменная, хранящая в себе все наборы убывающих последовательностей заданного ряда. Имя максимально неинформативно.

human1 - man_with_club
// из тетрадки, где планировал, как буду писать игру. Вместо говорящего имени использовал какие-то нелепости.

human2 - man_with_stone_axe
// оттуда же. Капелька стыда не помешает.