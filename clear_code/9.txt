if self.compare(value, self.head.value) in (0, -asc) // было
less_than_head = self.compare(value, self.head.value) in (0, -asc) // стало
// из класса OrderedList, функция add(self, value)

if self.compare(value, self.tail.value) in (0, asc) // было
more_than_tail = self.compare(value, self.head.value) in (0, -asc) // стало
// из класса OrderedList, функция add(self, value)

16 - DEFAULT_CAPAVITY
2 - DEFAULT_INCREASE
1.5 - DEFAULT_DECREASE
// три примера из DynamicArray. Числовые значения встречаются часто, лучше
// заменить их на константы с говорящими переменными

0 -_TOP_OUTLINE_LEVEL
// из программы, выкачивающей видео подписок с ютуба, есть парсинг xml-файла с подписками. Лучше указать, что там за магический ноль.

'last.txt' - _LAST
// оттуда же, имя файла, в котором хранится timestamp последнего запуска

'subs.xml' - _SUBS
// оттуда же, имя файла с подписками

os.getcwd() - _MAIN_DIR
// оттуда же, путь к папке, в которой запущена программа

-3 - _DEFAULT_DAYS_SUBSTRACT
// оттуда же, количество дней, которое требуется вычесть от текущей даты, если инициализация по дате проведена пользователем неправильно.

'OU=Pskov,dc=region,dc=cbr,dc=ru' - PSKOV_OU
//из скрипта по выборке пользователей из нашей рабочей ActiveDirectory.

'UTF-8' - DEFAULT_ENCODING
// Из программы про книги. Часто встречается упоминание кодировки по умолчанию.

UPLOAD_ALLOW - FILE_TYPES_ALLOWED_FOR_UPLOAD
//из старого фитнес-приложения, разработку которого прикрыли. Типы файлов, разрешённые для загрузки на сервер (приложение использовало
не все типы графических и видеофайлов), чтобы работники не перепутали, надо было отсекать ещё на фазе создания тренировочных программ.

'Неверный формат файла' - admin_page_values['current_lang']
//из старого фитнес-приложения, разработку которого прикрыли. Если возобновлять работу, то для каждого класса вместо конкретных строк
создавать словарик строковых значений, в котором под ключом 'rus', 'eng' и т.д. хранится то или иное строковое значение на указанном
языке.

self.capacity = int(self.capacity/1.5) //было-стало
// из класса DynArray, собственно, привести к целому типу не забыл, это скорее иллюстрация к тому, что давно заимел привычку приводить
результат деления к тому типу, к которому относился числитель (или знаменатель, по ситуации; в общем, ничего не брошено на самотёк).